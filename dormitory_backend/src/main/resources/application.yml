spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/dormitory_db
    username: postgres
    password: 19052004

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

logging:
  level:
    com.stu.dormitory: DEBUG
    org.hibernate.SQL: DEBUG

